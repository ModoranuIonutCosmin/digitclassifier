import { MyCustomPaginatorIntl } from './my-custom-paginator-intl';

describe('MyCustomPaginatorIntl', () => {
  it('should create an instance', () => {
    expect(new MyCustomPaginatorIntl()).toBeTruthy();
  });
});
